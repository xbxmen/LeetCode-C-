#include <climits>
#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <set>
#include <vector>

using namespace std;

int main() {

  vector<int> qwe = {1};
  int a = qwe.front();
  cout << a << endl;
  cout << 1111111111111111 << endl;

  cout << INT_MAX;

  // set<int> qwe = {11, 22, 33, 44};
  // for (auto it = qwe.begin(), itNext = qwe.begin(); it != qwe.end(); it++) {
  //   itNext++;

  //   cout << "it:" << *it << endl;
  //   cout << "itNext:" << *itNext << endl;

  //   if (itNext == qwe.end()) {
  //     cout << 111111111 << endl;
  //   }
  // }

  // int num = 10;
  // char str[100];
  // itoa(num, str, 2); // c++中一般用_itoa，用itoa也行,
  // printf("%s\n", str);
  return 0;
}